server:
  port: 7002

eureka:
  instance:
    hostname: eureka7002.com
  #    hostname: localhost # eureka服务端的实例名称 -- 单机版
  client:
    register-with-eureka: false # false 表示不向服务中心注册自己
    fetch-registry: false  # false 表示自己自己端就是注册中心，我的职责就是维护服务实例，并不需要去检索服务
    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/ # 设置与 Eureka Server交互的地址，查询服务和注册服务都需要依赖这个地址 -- 单机版
      defaultZone: http://eureka7001.com:7001/eureka/,http://eureka7003.com:7003/eureka/